import { SeeSingleBrand } from "@/app/components/SeeSingleBrand";
import { Metadata } from "next";

export default function SingleBrand () {
    
    return <SeeSingleBrand/>
} 


export const metadata: Metadata = {
    title: "Single Brand",
};
